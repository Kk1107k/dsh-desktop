# 截图目录（待补三张）

根目录 `README.md` 的「界面预览」按**固定文件名**引用这里的三张图。
**改文件名必须同步改 README**，否则图挂掉。

| 文件 | 内容 | 建议尺寸/状态 |
|---|---|---|
| `splash.png` | 开机页（鲸鱼 + 品牌字书写动画） | 480×320（splash 窗口原始尺寸） |
| `main-window.png` | 主窗口接管 `127.0.0.1:3080` 后的界面 | 1280×800 |
| `tray-menu.png` | 托盘右键菜单（六项） | 菜单展开状态 |

> 三张图**尚未提供**：README 里的引用当前会显示为占位 —— 这是有意保留的结构，
> 图放进来即自动生效，无需再改 README。
>
> 另外注意：仓库里的 `src/splash.html`、`src/update-dialog.html` 是**生成产物**，
> 截图要反映的是**生成后**的效果；改样式请改 `tools/gen_*.py` 后重跑，不要手改生成页。
