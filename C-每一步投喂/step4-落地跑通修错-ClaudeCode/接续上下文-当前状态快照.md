# 接续上下文 · 当前状态快照（2026-09-25 12:55 · 阶段 B 开工前）

> 换模型必须重开终端 → 新模型是失忆的。**第一条消息整段粘贴本文件**。
> ⚠ 本文件会过期：粘贴前先 `git log --oneline -6` 核对，状态不符以实际为准。

---

```text
这是 DSH Desktop（Electron 桌面壳）的接续任务。项目根 D:\dsh-desktop。
你是执行工程师，架构基线 B-AI共享/SPEC.md 已冻结（12 节 376 行），不要重做架构决策。

【已完成 · 阶段 A 全部收尾，不要回退】
最近提交：
  d32c457 移除 --test-force-exit 止血开关
  1e60c92 tray 标准模式项显式写 enabled: true
  72257a6 dsh-host 强制回收路径改为等待真实 exit 事件
  728b7ba 修测试夹具两处缺陷（A04/A05/A08 七条失败全消）
  4d6aac0 updater loadSnooze 不再用 | 0 截断毫秒时间戳（真 bug：snooze 曾恒失效）
  3fca89b tsconfig 关闭 noImplicitAny/strictNullChecks 降噪（178 → 7）
  7feb0a4 配置国内镜像 + onlyBuiltDependencies，装通 electron 二进制
已修且不许改回去的：
  - electron-builder.yml 的 NSIS 选项必须是 allowToChangeInstallationDirectory
  - electron-builder.yml 的 files 保留 !assets/fonts/_archive/** 与 !assets/_archive-*
  - src/preload.js 里的 require 是对的（会被 esbuild 打成 build/preload.cjs），不要改成 import
  - src/splash.html、src/update-dialog.html 是生成产物，只能改 tools/gen_*.py 后重跑
  - package.json 的 test 脚本保持 node --test tests/acceptance.test.mjs，不要加 --test-force-exit
    （留着会掩盖未来新的句柄泄漏）

【当前实测状态】
  - electron 二进制：已就位（node_modules/electron/dist/electron.exe）
  - .npmrc + pnpm-workspace.yaml：已就绪（国内镜像 + onlyBuiltDependencies）
  - npx tsc --noEmit：剩 7 个 —— TS2551×2（onTraySetText 命名漂移）、TS2353×2（main.js:40
    schemaVersion / main.js:154 config 缺 JSDoc 字段）、TS2561（main.js:172 onTrayState 死代码）、
    TS2540（updater.js:86 signals 只读）、TS2339（updater.js:268 .message on unknown）
  - pnpm test：31 通过 / 0 失败，13.5s 自然退出
  - 阶段 B 尚未开始：pnpm dev / pnpm build 都没跑过

【硬性约束】
  - preload 契约的 11 个方法名一个字都不许改：splashAPI(onStatus/onFinish/getVersion/
    minimize/close)、updateAPI(onState/getState/startDownload/retry/close/snooze)
  - 不改 D1–D5：min-display 2400ms / 淡出 300ms / snooze 24h / 版本来自 app.getVersion() / 产物 dist/
  - 不许为了"让窗口能显示"放宽安全约束（CSP / sandbox / contextIsolation / webSecurity）
  - 占位符 <占位 COS 域名> / <占位官网域名> 保持原样，不要编造
  - 上游 CLI 与 SPEC 不符 → 记到 SPEC.md §11.1，不许静默改壳侧契约
  - 一律 pnpm，不要 npm install；每完成一个可提交单元就 git commit
```
