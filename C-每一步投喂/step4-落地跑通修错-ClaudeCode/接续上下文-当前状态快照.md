# 接续上下文 · 当前状态快照（2026-09-25 02:20 · 换 DeepSeek 轮）

> 换模型必须重开终端 → 新模型是失忆的。**第一条消息整段粘贴本文件**，
> 它就不用重新探索项目，也不用你重新解释。

---

```text
这是 DSH Desktop（Electron 桌面壳）的接续任务。项目根 D:\dsh-desktop。
你是执行工程师，架构基线 B-AI共享/SPEC.md 已冻结（12 节 376 行），你不要重做架构决策。

【已完成 · 不要回退、不要重做】
最近 6 个提交：
  c49c8fa 验收测试 A01~A10（mock 层，stub electron/electron-updater + fake-npx）
  9abcaad 补齐打包发布四文件（build-preload.mjs / after-pack.cjs / release.mjs / release.yml）
          + package.json 加 pnpm.onlyBuiltDependencies
  923a1b6 JSDoc 类型标注（preload/main-window/tray 已零错误，updater 部分完成）
  0c4e513 消除 ESM 裸 require；修 logger 的 (?i) 正则语法错误等错误路径缺陷
  5a93034 dsh-host 落实 locateRuntime（注册表→where node→常见目录，校验 ≥22，E_RUNTIME_MISSING）
已修且不许改回去的：
  - electron-builder.yml 的 NSIS 选项必须是 allowToChangeInstallationDirectory（写成 ...Location 是错键）
  - electron-builder.yml 的 files 保留 !assets/fonts/_archive/** 与 !assets/_archive-*
  - src/preload.js 里的 require 是对的（会被 esbuild 打成 build/preload.cjs），不要改成 import
  - src/splash.html、src/update-dialog.html 是生成产物，只能改 tools/gen_*.py 后重跑

【硬性约束】
  - 接口名一个字都不许改：splashAPI(onStatus/onFinish/getVersion/minimize/close)、
    updateAPI(onState/getState/startDownload/retry/close/snooze) —— 改名会导致静默失效
  - 不改 D1–D5：min-display 2400ms / 淡出 300ms / snooze 24h / 版本来自 app.getVersion() / 产物 dist/
  - 占位符 <占位 COS 域名> / <占位官网域名> 保持原样，不要编造
  - 一律 pnpm，不要 npm install
  - 每完成一个可提交单元就 git commit（重开终端后 git log 是唯一能继承的记忆）

【当前状态 · 我已实测，不用你重新发现】
  1) npx tsc --noEmit 报 178 个错误，分布：
     TS2339=43 / TS7006=35 / TS7005=34 / TS18047=32 / TS2322=11 / TS7034=10 / TS7031=4 / TS2551=2
     集中在 src/updater.js 与 src/main.js
  2) node --test tests/acceptance.test.mjs 已跑过一次：31 个用例 20 通过 11 失败
     失败名单（逐个）：
       A04 GitHub 有新版 → available（不自动下载）
       A04 无新版 → latest + 托盘 5s 文字反馈
       A05 GitHub 网络失败 → 真实转向 COS 实例且不降版本
       A05 双源均失败 → error + 手动下载入口
       A06 重启后 snooze 未到期 → 自动检查被抑制
       A08 下载中 GitHub 失败 → COS 元数据一致才续接；下载完成先停 host 再安装
       A08 COS 元数据版本不一致 → E_MIRROR_MISMATCH
       A08 COS 下载阶段失败 → error，不安装
       A09 host 忽略 shutdown → 3s 后进程树强制回收，不触发重启
       A09 托盘菜单六项顺序正确；未实现模式禁用且仅标准可勾选
       A10 release.mjs 缺少 COS 凭据 → 失败退出
  3) electron 二进制未安装：node_modules/electron 包在（36.9.5），
     但 node_modules/electron/dist/electron.exe 与 path.txt 都不存在
  4) tsconfig.json 目前只有 "strict": true，没有 noImplicitAny / strictNullChecks 覆盖
  5) 已知偏差：src/about.html 里写死版本 0.1.0，违反 SPEC 的 D4（版本须来自 app.getVersion()）

【本轮任务 · 严格按顺序，每步先报告再动手；一次只做一步，做完 commit】

第 1 步（硬阻塞，先做）：装 electron 二进制
  根因已定位：electron postinstall 要从 github.com 下载约 100MB 二进制，国内下不动；
  pnpm 12 又拦截构建脚本。已实测国内镜像可达（npmmirror.com/mirrors/electron/ 返回 302）。
  a. 新建项目根 .npmrc，内容三行：
     registry=https://registry.npmmirror.com
     electron_mirror=https://npmmirror.com/mirrors/electron/
     electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
  b. 用镜像直接跑 electron 自己的安装脚本（绕开 pnpm 脚本拦截）：
     PowerShell: $env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"; node node_modules/electron/install.js
  c. 新建 pnpm-workspace.yaml，写入 onlyBuiltDependencies: electron / electron-winstaller / esbuild
     （pnpm 10.6+ 的规范位置；不要删掉它当成占位内容）
  d. 验证 node_modules/electron/dist/electron.exe 存在，且 node -p "require('electron')" 返回 exe 路径
  e. commit

第 2 步：降噪类型检查
  tsconfig.json 保留 "strict": true，新增 "noImplicitAny": false 与 "strictNullChecks": false。
  重跑 npx tsc --noEmit，报告剩余错误数 + 按错误码和文件各列一张表。
  （预期能消掉约 156 个"缺类型标注"型错误，剩下才是真问题）

第 3 步：测试失败归类（只报告，不要改代码）
  把上面 11 个失败逐个归类为：代码 bug / 测试期望与实现不一致 / 环境依赖。
  注意 A09 的两个怀疑是环境相关（locateRuntime 会查注册表），请在你这边复跑确认。
  报告完等我确认再动手。

第 4 步：等确认后才允许进入阶段 B（pnpm dev / pnpm build）。

【报告格式】保持简短：改了什么文件、命令输出关键行、下一步建议。不要长篇复述。
```
