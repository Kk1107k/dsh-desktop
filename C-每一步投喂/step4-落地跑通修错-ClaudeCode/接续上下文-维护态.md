# 接续上下文 · 维护态（2026-09-25 归档）

> 用途：**新开终端继续本项目的任何工作**时，第一条消息整段粘贴本文件。
> 项目状态：**首版已发布，进入维护**。不是开发中。
> 📄 完整背景见 `A-我读的/项目总结报告.md`（错误盘点与经验都在那）。

---

```text
DSH Desktop（`D:\dsh-desktop`）—— `@deepseek-ai/dsh` 的**独立非官方** Windows Electron 壳。
仓库 `github.com/Kk1107k/dsh-desktop`（public，MIT）。
**当前版本 v0.1.7-rc.2，已发布**（GitHub Release + COS 国内备源均已就位），装包实测通过。
现在处于【维护态】：除非明确要求，不要主动改功能。

【已完成的全部工作（不要回退、不要"重做"）】
- 阶段 A/B 全部完成；三审通过；装包实测（安装→使用→卸载）通过
- 上游已绑定 `@deepseek-ai/dsh@0.1.7-rc.2`；版本号规则 = **壳版本 = 绑定的 dsh 版本**（官方铁律）
- 7 项对齐官方 Desktop 的动作已完成；第 8 步文档收尾已完成
- 三处 COS 域名同源（electron-builder.yml / updater.js / after-pack.cjs），有断言防漂移
- 产物名无空格（`DSH-Desktop-Setup-${version}.${ext}`）
- 本地干跑 `node tools/release.mjs --plan-only` 可用（不读凭据不上传）

【硬性约束（不变）】
- preload 契约 11 个方法名一个字不许改；D1–D5 不许动
- `src/splash.html` / `src/update-dialog.html` 是生成产物，改 `tools/gen_*.py` 后重跑
- README 第 11–16 行的「非官方外壳 + 商标免责声明」**不得删改**
- 停壳优先走托盘「退出」；必须强杀时 `taskkill /PID <主进程> /T /F`（**带 /T**）
- **禁止任何形式的用户屏幕/前台窗口抓取**（隐私边界，已写入项目硬约束）
- 命令行装包**必须带 `/D=<路径>`**（不带会把安装目录整个卸掉 —— 已发生过一次）
- `A-我读的/AUDIT.md` 只由审计方写入
- 一律 pnpm；每完成一个可提交单元就 commit

【下次发版的标准动作】
  ① 改 package.json 的 version（= 绑定的 dsh 版本）
  ② pnpm build（带 ELECTRON_MIRROR + ELECTRON_BUILDER_BINARIES_MIRROR 两个环境变量）
  ③ node tools/release.mjs --plan-only     ← 本地先验，必须 EXIT=0
  ④ git tag v<版本> → git push origin main → git push origin v<版本>
  ⑤ 发布后核：GitHub Release 的 4 个产物 + COS 的 cn-stable.yml（应 404→200）
  ⚠ 顺序：先推代码再推 tag（否则 CI 拿旧代码）

【已知限制（不是缺陷）】
1. 未签名 —— SmartScreen 会提示；与官方铁律②「已签名更新单元」有有意差异
2. 依赖系统 Node ≥22 —— 官方是内置运行时；已登记为架构偏离
3. 更新链路（A04 available / A08 下载安装）尚未真机验证 —— 需发更高的 test 版才能触发

【维护时的常见任务】
- 上游 dsh 发新版 → 按官方铁律必须发新 Desktop，且**重跑 §11.1 的 8 条重验**
- 改 UI/文案 → 改 `tools/gen_*.py` 后重跑，不要手改生成页
- 改打包配置 → 注意三处域名同源的断言
- 改 release 流程 → 先确认 `--plan-only` 仍能 EXIT=0
```

---

## 关键文件位置

| 文档 | 用途 |
|---|---|
| `A-我读的/项目总结报告.md` | **先看这个** —— 项目总览 + 错误盘点 + 经验 |
| `A-我读的/AUDIT.md` | 完整审计记录 |
| `A-我读的/官方Desktop学习报告.md` | 官方 Desktop 调研与架构差异 |
| `A-我读的/执行流程-Runbook.md` | step0~step9 流程 |
| `B-AI共享/SPEC.md` | 冻结规格（含 §11.1 实测与适配） |
| `C-Users/K/AppData/Roaming/dsh-desktop/logs/main.log` | 运行日志 |
