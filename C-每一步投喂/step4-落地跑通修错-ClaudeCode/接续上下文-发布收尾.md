# 接续上下文 · 发布收尾阶段（2026-09-25 22:04）

> 用途：上一个终端上下文达 98%，需**新开终端**继续。
> **新终端第一条消息整段粘贴本文件**。
> ⚠ 本文件可能滞后：粘贴前先 `git log --oneline -8` 与 `git status` 核对，**以实际状态为准**。

---

```text
DSH Desktop（`D:\dsh-desktop`）已进入**发布收尾**。架构基线 `B-AI共享/SPEC.md` 已冻结。

【当前阶段】发布前的最后一批动作，做完即可打 tag 发布。

【已完成（不要回退）】
- 阶段 A/B 全部完成；三审通过；装包实测通过（安装→使用→卸载闭环）
- 7 项对齐官方 Desktop 的动作已完成；第 8 步文档收尾（README/CHANGELOG/about）已完成
- **上游已升级**：TARGET_PKG = `@deepseek-ai/dsh@0.1.7-rc.2`；package.json version = `0.1.7-rc.2`
  （版本号规则：**壳版本 = 绑定的 dsh 版本**，依据官方铁律；§11.1 已登记）
- §11.1 的 8 条重验已完成（7 条一致、⑦ registry 已修：壳固定下发 `npm_config_registry`）
- **COS 桶已就绪**：桶名 `dsh-desktop-1432719119`、域名
  `dsh-desktop-1432719119.cos.ap-shanghai.myqcloud.com`、region `ap-shanghai`
  GitHub Secrets 已填 4 个：COS_SECRET_ID / COS_SECRET_KEY / COS_BUCKET / COS_REGION
  权限"公有读私有写"已实测验证（返回 404 NoSuchKey ⇒ 匿名读生效）
- 三张截图已入仓：`assets/screenshots/{splash,main-window,tray-menu}.png`

【本批（发布收尾）要做的三件事 —— 先核对哪些已完成】
1. **填真实 COS 域名**（替换占位符）：
   - `electron-builder.yml` 的 publish generic url → `https://dsh-desktop-1432719119.cos.ap-shanghai.myqcloud.com/dsh-desktop`
   - `src/updater.js` 的 COS feed URL 默认值（已是可注入形态）
   - `SPEC.md` 的占位符是**冻结文本**：行尾标注"已被 §11.1 修订"+ 登记，**不要改写冻结句**
   - ⚠️ **保留"备源未配置"的容忍分支**（将来配置缺失时的兜底，别删）
2. **排除截图**：`electron-builder.yml` 的 files 加 `- "!assets/screenshots/**"`
   （文档素材不该进安装包；当前包里确实有 ~96KB）+ 补一条断言
3. **重跑构建**（带 `ELECTRON_MIRROR` + `ELECTRON_BUILDER_BINARIES_MIRROR` 两个环境变量）
   核验：① asar 内无 screenshots ② asar 内有新域名 ③ 源码 grep "占位 COS 域名" 命中 0

【之后】打 tag `v0.1.7-rc.2` → 触发 CI 发布 → 验 A10（GitHub/COS 哈希一致、COS 元数据最后发布）
⚠️ tag 必须**等于** package.json 的版本（workflow 会校验）

【硬性约束（不变）】
- preload 契约 11 个方法名一个字不许改；D1–D5 不许动
- `src/splash.html` / `src/update-dialog.html` 是生成产物，改 `tools/gen_*.py` 后重跑
- 停壳优先走托盘「退出」；必须强杀时 `taskkill /PID <主进程> /T /F`（**带 /T**）
- **禁止任何形式的用户屏幕/前台窗口抓取**（隐私边界）
- 装包测试若用命令行安装：**必须带 `/D=<路径>`**（不带会把安装目录整个卸掉）
- `A-我读的/AUDIT.md` 只由审计方写入
- 一律 pnpm；每完成一个可提交单元就 commit

【待办（发布跑通后）】
- 收紧 COS 子账号权限：现在用预设 `QcloudCOSDataFullControl`（**全账号 COS 级，含删除**，
  能碰凯哥博客的桶）→ 换成限定单桶 + 不含 `cos:DeleteObject` 的自定义策略
- 可选：GitHub 发一个更高版本 release 以验证 A04（有新版 → available）
```

---

## 附：常用命令与位置

| 项 | 值 |
|---|---|
| 项目根 | `D:\dsh-desktop` |
| 日志 | `C:\Users\K\AppData\Roaming\dsh-desktop\logs\main.log` |
| 安装包 | `dist\DSH Desktop-Setup-0.1.7-rc.2.exe` |
| 审计报告 | `A-我读的/AUDIT.md` |
| 官方 Desktop 调研 | `A-我读的/官方Desktop学习报告.md` |
| 装包验收流程 | `A-我读的/装包验收流程.md` |
