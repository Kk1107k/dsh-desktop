# Step 6 · 文档收尾与发布（国内版 + 本地命令）

> 这一步**没有独立 prompt**：体力活，直接给国内版下指令即可。

| 项 | 内容 |
|---|---|
| **投给谁** | WorkBuddy **国内版** Agent |
| **模型** | 国产任选 |
| **思考强度** | **low / 关思考** |
| **预算** | ~100 积分 |
| **产出** | README 补齐截图与下载链接、`CHANGELOG.md`、`src/about.html` |

## 给国内版的指令（可直接复制）

```text
按 SPEC.md 第 12 节产出：
1. 补齐 README.md：特性、截图位、下载链接、开发命令（必须用 pnpm）、QA
   ⚠ 必须保留并放在显眼位置的"非官方外壳 + 商标免责声明"段落，不得删改
2. CHANGELOG.md：从 v0.1.0 起
3. src/about.html：关于页（应用名、版本、上游链接、免责声明）
```

## 发布（本地执行，不投喂）

```bash
git tag v0.1.0 && git push --tags     # 触发 release workflow
```

产物在 `dist/`：`DSH Desktop-Setup-<version>.exe`
→ 上传 GitHub Releases → 同步腾讯云 COS `ap-shanghai`（路径见 `B-AI共享/INTERFACE.md` §6）。

> ⚠ 国内分发别跳：GitHub 在你网络里大概率拉不动，**失败 = 用户永远卡在旧版本**。

## 收尾预算核对

国际版 140（余 300） / 国内版 1000（余 2000） / Claude Code API ¥18~45。
