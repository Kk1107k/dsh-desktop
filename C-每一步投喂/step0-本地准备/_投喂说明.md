# Step 0 · 本地准备（不投喂给任何 AI）

✅ **已于 2026-09-24 完成**：`git init` + 首次提交 `342fb9d`，62 个文件全部入库，工作区干净。
下面的内容保留供复查/重建环境时参考。

## 做了什么

```bash
cd D:\dsh-desktop
git init
git config core.autocrlf false     # 生成产物与 HTML 不做 CRLF 转换，避免 diff 被换行符污染
git add -A
git commit -m "chore: 交付素材与契约（页面/图标/prompt/INTERFACE/README）"
```

首次提交：**`342fb9d`** · 62 files changed · 分支 `master` · 工作区 0 处改动。
提交身份继承全局配置 `Kk1107k <Kk1107k@users.noreply.github.com>`（与 GitHub 一致）。

## 为什么必须先 git 化

三份 AI 之间没有共享上下文，**仓库里的文件就是它们唯一的共享内存**。
不 git init，后面每一步的产出都没有落点，AI 之间也无从对齐。

## 检查清单（实测 2026-09-24 全部通过）

- [x] `README.md`（含商标免责声明）
- [x] `B-AI共享/INTERFACE.md`（接口契约）
- [x] `src/splash.html`、`src/update-dialog.html`
- [x] `assets/whale.svg`、`assets/icon.ico`、`assets/tray*.ico`
- [x] `C-每一步投喂/step0…step6/` 各自的 prompt 或说明
- [x] `.gitignore` 不误伤资源 —— 用项目副本 `git add -A --dry-run` 实测 **62/62 全部纳入**

## 环境（实测达标）

| 项 | 要求 | 实测 |
|---|---|---|
| git | 有 | 2.55.0 ✓ |
| node | ≥ 22 | v22.22.2 ✓ |
| pnpm | ≥ 9 | 12.5.1 ✓ |
| fontTools | 有（页面生成） | 4.65.0 ✓ |
| Chrome/Edge | 有（图标光栅化） | Chrome ✓ |

## 耗时 / 花费

约 2 分钟，0 积分。

## 下一步

进 `../step1-架构spec-国际版/`，把 prompt-A + README + INTERFACE 三份一起投给国际版 GPT-6（high 档）。
