# Step 0 · 本地准备（不投喂给任何 AI）

## 做什么

```bash
cd dsh-desktop
git init && git add . && git commit -m "chore: 交付素材与契约（页面/图标/prompt/INTERFACE）"
```

## 为什么必须先 git 化

三份 AI 之间没有共享上下文，**仓库里的文件就是它们唯一的共享内存**。
不 git init，后面每一步的产出都没有落点，AI 之间也无从对齐。

## 检查清单

- [ ] `README.md`（含商标免责声明）
- [ ] `B-AI共享/INTERFACE.md`（接口契约）
- [ ] `src/splash.html`、`src/update-dialog.html`
- [ ] `assets/whale.svg`、`assets/icon.ico`、`assets/tray*.ico`
- [ ] `C-每一步投喂/step1…step6/` 各自的 prompt 或说明

## 耗时 / 花费

约 2 分钟，0 积分。
