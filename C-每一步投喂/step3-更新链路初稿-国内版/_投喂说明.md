# Step 3 · 更新链路初稿（投给国内版 Agent）

> 这一步**没有独立的 prompt 文件**——它是 SPEC 第 7 节（双通道状态机）与
> 第 11 节（打包发布）的直接翻译，用下面的说明当 prompt 即可。

| 项 | 内容 |
|---|---|
| **投给谁** | WorkBuddy **国内版** Agent |
| **模型** | GLM-5.3 或 DeepSeek |
| **思考强度** | **medium** |
| **预算** | ~150 积分 |
| **产出** | `src/updater.js`（M08）与 `.github/workflows/release.yml`（M11）初稿 |

## 这一步要粘进去的内容

1. **`../../B-AI共享/SPEC.md` 的第 7 节 + 第 11 节**（只粘这两节即可，不用整篇）
2. `../../B-AI共享/INTERFACE.md` 的 §6（发布产物路径）
3. 下面这段要求：

```text
请按上面 SPEC 第 7 节的状态机，逐条翻译成 src/updater.js（electron-updater v6）：
- 双通道降级：GitHub Releases 失败 → 切 cn-stable（腾讯云 COS）→ 再失败则提示手动下载
- 失败判定正则：/timeout|ETIMEDOUT|ENOTFOUND|cloudflare|404/i
- 必须实现 INTERFACE §5 的 snooze(24h)：userData/update-snooze.json 存 { until: epoch ms }
- 再按 SPEC 第 11 节产出 .github/workflows/release.yml
- 产物路径统一 dist/，安装包名 <productName>-Setup-<version>.exe
```

## 验收点

- [ ] 失败正则在位
- [ ] COS 同步步骤指向 `dist/`（不是任何 `assets/release/`）
- [ ] snooze 有真实存储实现

## 下一步

`../step4-落地跑通修错-ClaudeCode/`（交给 Claude Code 真正跑起来）
