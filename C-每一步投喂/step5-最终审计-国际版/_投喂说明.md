# Step 5 · 最终审计 —— 改为本地执行（0 积分）

> **2026-09-24 变更**：国际版 440 积分在 step1 已用光（P0 单条扣 ~59，失败也扣、重试再扣）。
> **本步不再投喂国际版 GPT**，改由我（WorkBuddy）或 Claude Code + GLM-5.3 在本地执行。

## 为什么这不降质量

审计不是开放式架构判断，而是**照 SPEC §12 的 A01~A10 逐条核对代码**。
这类活儿由"能直接读代码、跑 grep、看 git 历史"的执行方来做，比对话式模型更准——
GPT 的优势在凭空推演架构，不在核对既有实现。

## 怎么做

1. 打开 `B-AI共享/SPEC.md` §12（A01~A10 十条可执行断言）
2. 逐条对照仓库代码核对，产出 `A-我读的/AUDIT.md`：
   每条给 **通过 / 需修 + 原因 + 修复优先级（P0/P1/P2）**
3. 额外必审一条：**契约一致性** ——
   preload 暴露的命名空间与 `B-AI共享/INTERFACE.md` §1 页面实际调用的完全对得上，
   含 SPEC 新增的 `updateAPI.snooze()`、`splashAPI.close()` 在淡出 300ms 后被调用

## 可复用资产

- `prompt-D-国际版-最终审计.md`：**不投喂**，但里面的审计点清单可直接当 checklist 本地照查
- `B-AI共享/INTERFACE.md`：契约对照表

## 若将来想让 GPT-6 二审

等充值或开 Pro 试用（今天 $0 + 500 积分，**第 6 天前必须取消**，否则扣 $10）后再跑一次；
二审前先把本地审计发现的问题修完，别把额度花在已知问题上。
